let person = {
    name: "Alex",
    age: 32,
    occupation: "Developer",
    favoriteCity: "Amsterdam",
    hobbies: ["Work out", "cook", "read", "code", "go for walks"],
};

console.log(`${person.name} is ${person.age} years old`);
console.table(person.hobbies);